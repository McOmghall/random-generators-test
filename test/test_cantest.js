const assert = require('assert')
const describe = require('mocha').describe
const it = require('mocha').it

describe('Test for can test', function () {
  it('should be able to assert', function () {
    assert.ok(true)
  })
})